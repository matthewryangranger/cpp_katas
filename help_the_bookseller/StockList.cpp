#include <map>
class StockList
{
public:
    static std::string stockSummary(std::vector<std::string>& books, std::vector<std::string>& categories){
        std::string return_str;
        if (books.size() == 0 || categories.size() == 0) {
            return_str = "";
        } else {

            for (std::string s: categories){

            }


            // Create the string
        }

        return return_str;
    }
};